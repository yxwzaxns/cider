version: '2'
services:
  cider:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8080:80
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    restart: always
    environment:
      - CIDER_PASSWORD=${CIDER_PASSWORD}
